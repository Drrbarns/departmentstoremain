/**
 * UI design toggles.
 *
 * Flip `USE_NEW_DESIGN` to `false` to instantly restore the previous
 * (classic) header + homepage hero. No other change is needed — both the
 * Header switcher and the homepage read this single flag.
 */
export const USE_NEW_DESIGN = true;
